[project]
name = "wordpress-mcp-server"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "mcp",
    "httpx",
    "python-dotenv",
    "protobuf>=7.34.1",
    "adk>=0.0.5",
]
